# Configuração do WordPress Database
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Prefixo das tabelas WordPress (usado em testes e desenvolvimento)
WP_DB_PREFIX=wpac_

# Configuração da Base de Dados (para testes locais)
WP_DB_NAME=wordpress_test
WP_DB_USER=root
WP_DB_PASSWORD=root
WP_DB_HOST=localhost

# Configuração do WordPress (para testes locais)
WP_VERSION=latest
WP_SITE_URL=http://localhost:8080

# Configuração PHP (para testes)
PHP_VERSION=8.1

# Google Sheets API (configurar no painel WordPress)
# Estes valores são gerenciados através da interface admin do WordPress
# SHEETS_API_KEY=sua-api-key-aqui
# SHEETS_SPREADSHEET_ID=seu-spreadsheet-id-aqui
# SHEETS_RANGE=Sheet1

# API Token do Plugin (gerado automaticamente no WordPress Admin)
# Não coloque valores reais aqui - este é apenas um exemplo
# OPORTUNIDADES_API_TOKEN=seu-token-de-32-caracteres-aqui
